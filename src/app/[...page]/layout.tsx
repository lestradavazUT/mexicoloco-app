export const metadata = {
    title: 'Mexico Loco - 404',
    description: 'Mexico Loco - Page not found. 404'
};

export default function NotFoundLayout({ children }) {
    return <>{children}</>;
}
